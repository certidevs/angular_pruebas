import { NumberValueAccessor } from '@angular/forms';
export interface Restaurant {
    id: number;
    name: string;
    location: string;
    rating: string;
    phone: string;
    imageUrl: string;


}